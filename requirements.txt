# Hash Identifier - Python Dependencies
# Minimal dependencies - designed to work offline with just Python stdlib

# Optional: For enhanced features (not required for basic functionality)
# colorama==0.4.6  # For colored terminal output (optional)
# tabulate==0.9.0  # For better table formatting (optional)

# Core functionality uses only Python 3.6+ standard library
# No external dependencies required for offline operation
